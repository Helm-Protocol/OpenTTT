import { SignatureFormat } from "@turnkey/api-key-stamper";
import type { TStamp, ApiKeyStamperBase } from "../../../__types__";
export declare class ReactNativeKeychainStamper implements ApiKeyStamperBase {
    private serviceName;
    listKeyPairs(): Promise<string[]>;
    createKeyPair(externalKeyPair?: {
        publicKey: string;
        privateKey: string;
    }): Promise<string>;
    deleteKeyPair(publicKeyHex: string): Promise<void>;
    private getPrivateKey;
    stamp(payload: string, publicKeyHex: string): Promise<TStamp>;
    sign(payload: string, publicKeyHex: string, format: SignatureFormat): Promise<string>;
}
//# sourceMappingURL=stamper.d.ts.map