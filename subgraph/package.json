{
  "name": "openttt-subgraph",
  "license": "BSL-1.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ helm-protocol/openttt-sepolia",
    "create-local": "graph create --node http://localhost:8020/ helm-protocol/openttt-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ helm-protocol/openttt-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 helm-protocol/openttt-sepolia"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
